// Package mcp owns canonical standalone MCP server desired semantics.
//
// It owns authored server identity, target/scope bindings, closed transport
// variants, ambient or exact-path command references, environment references,
// local parameter references, and requested absence policy. It does not own
// host support matrices, config syntax, carrier contributions, runtime values,
// probing, provisioning, or mutation routes.
package mcp
